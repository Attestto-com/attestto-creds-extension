export interface LinkedToken {
  mint: string
  symbol: string
  name: string
  balance: number
  rawBalance: string
  decimals: number
  tokenProgram: string
  isToken2022: boolean
  image?: string
}
